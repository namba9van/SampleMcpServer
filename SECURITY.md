# Security

This project can execute autonomous actions when explicitly configured. External capabilities are disabled by default and should be enabled only for endpoints and workspaces you trust.

Before deployment, review [docs/SECURITY.md](docs/SECURITY.md) and `.env.example`.

Do not report real credentials in public issues. Rotate any credential that may have been exposed and remove it from Git history before publishing.
